/**
 * Public API of the `inventory` module. Cart/orders read availability and
 * reserve/confirm/release stock via `InventoryService` — never the entities.
 */
export { InventoryModule } from './inventory.module';
export { InventoryService } from './services/inventory.service';
