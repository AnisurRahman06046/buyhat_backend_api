/** Where a banner renders. Hero slides are banners with `HOME_HERO` placement. */
export enum BannerPlacement {
  HOME_HERO = 'HOME_HERO',
  HOME_STRIP = 'HOME_STRIP',
  CATEGORY = 'CATEGORY',
  ANNOUNCEMENT = 'ANNOUNCEMENT',
}
