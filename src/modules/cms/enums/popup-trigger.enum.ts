/** When a popup is shown (enforced client-side; server just stores the rule). */
export enum PopupTrigger {
  ON_LOAD = 'ON_LOAD',
  AFTER_DELAY = 'AFTER_DELAY',
  EXIT_INTENT = 'EXIT_INTENT',
  ON_SCROLL = 'ON_SCROLL',
}
